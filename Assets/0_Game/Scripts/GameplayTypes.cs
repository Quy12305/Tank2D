public enum BulletType
{
    Normal,
    Laser,
    Freeze
}

public enum BotBehaviorType
{
    Smart,
    Dumb,
    Sentry
}

public enum CellType
{
    Empty = 0,
    SolidWall = 1,
    BreakableWall = 2
}
